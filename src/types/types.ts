export type CardType = {
    name: string;
    link: string;
    description: string;
}

export type SocialLinks = {
    name: string;
    link: string;
}

export type Email = {
    name: string;
    link: string;
}
